# Interim root ignores (phase 1 of the step-10 cutover) — replaced by the
# promoted web/.gitignore in phase 2.
.DS_Store
node_modules

# Claude Code project dir: only skills/ is tracked
.claude/*
!.claude/skills/
